spring:
  application:
    name: gateway
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8769
zuul:
  routes:
    api-a:
      path: /auth/**
      serviceId: auth-service
    api-b:
      path: /account/**
      serviceId: account-service